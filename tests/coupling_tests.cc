#include "catch.hpp"
#include "coupling.h"

TEST_CASE("Evaluating coupling", "[linear coupling]"){
	int n_vars = 3;
	connection
	
	coupling = linear_coupling(1.0,0.0);
	
	connectivity

	global_state

	global_coupling

	coupling(connectivity, global_state, global_coupling)
	//REQUIRE( h_bufs.get_value_at(0,2) == Approx(9) );
}
